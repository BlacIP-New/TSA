/**
 * Audit Service — Integration stubs for Credo backend audit log endpoints.
 *
 * TODO (Backend Integration):
 *   GET /admin/audit-log
 *     params: { aggregatorId, from?, to?, action?, userId?, page, limit }
 *     → { data: AuditEntry[], total: number, page: number, totalPages: number }
 */

export { };
